"""
Phase 2 Extraction Guide

This file documents exactly what needs to be extracted from terractl.py
to complete the modular command structure.

COMPLETED:
- lib/commands/__init__.py (empty - ready for imports)

TODO - Extract these commands from terractl.py:

1. clean.py (lines 202-218)
   - Helper: None
   - Main: cmd_clean(args)
   
2. install.py (lines 45-197)
   - Helpers: auto_detect_path, prompt_path
   - Main: cmd_install(args)

3. build.py (lines 223-480)
   - Helpers: parse_localization_file, merge_localization, build_actors, 
              build_templates, create_templates_db
   - Main: cmd_build(args)
   - Imports: json, csv, sqlite3, Path, logging
   - Dependencies: lib.core (load_env, get_project_root), lib.performance (timed_command)

4. parse.py (lines 485-528)
   - Helper: find_savegame
   - Main: cmd_parse(args)
   - Imports: json, gzip, sqlite3, Path, logging
   - Dependencies: lib.core, lib.performance

5. inject.py (lines 533-617)
   - Helper: load_actors
   - Main: cmd_inject(args)
   - Imports: json, sqlite3, Path, datetime, logging
   - Dependencies: lib.core, lib.performance, lib.launch_windows

6. validate.py (lines 622-754)
   - Helper: None
   - Main: cmd_validate(args)
   - Imports: os, Path, logging
   - Dependencies: lib.core, lib.performance

7. run.py (lines 759-808)
   - Helper: None
   - Main: cmd_run(args)
   - Imports: os, subprocess, Path, logging
   - Dependencies: lib.core (load_env), inject.cmd_inject
   - Note: NOT timed (launches external process)

8. perf.py (lines 813-858)
   - Helper: None
   - Main: cmd_perf(args)
   - Imports: Path, collections.defaultdict
   - Dependencies: lib.core (get_project_root)

9. CREATE tias.py (new file, ~100 lines)
   - Imports all command modules
   - Argparse setup
   - Command dispatch
   - Entry point

THEN:
- Test all commands work
- Delete terractl.py
- Update README.md (terractl.py → tias.py)
- Commit Phase 2

Estimated time: 1-2 hours with fresh token budget
"""
